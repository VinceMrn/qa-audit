{
  "name": "qa-audit-dev",
  "private": true,
  "type": "module",
  "description": "Dev tooling for the qa-audit plugin. Not published, not required to use the plugin.",
  "scripts": {
    "test": "node --test \"tests/*.test.mjs\"",
    "check": "node --check plugins/qa-audit/scripts/build-report.mjs && node --check plugins/qa-audit/scripts/generate-spec.mjs"
  },
  "engines": { "node": ">=18" }
}
